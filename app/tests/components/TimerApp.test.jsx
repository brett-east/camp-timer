import React from 'react';
import ReactDOM from 'react-dom';
import TestUtils from 'react-addons-test-utils';
import expect from 'expect';

import TimerApp from 'TimerApp';

describe('TimerApp', () => {
  it('should exist', () => {
    expect(TimerApp).toExist();
  });
});
