import React from 'react';
import ReactDOM from 'react-dom';
import TestUtils from 'react-dom/test-utils';
import expect from 'expect';

import ManualTime from 'ManualTime';

describe('ManualTime', () => {
  it('should exist', () => {
    expect(ManualTime).toExist();
  });
});
